# Copy this file to `.env` (same folder) to enable AI strategy authoring, then
# restart the engine:  python server.py
#
# Without a key the engine still runs — AI authoring just reports unavailable.

ANTHROPIC_API_KEY=sk-ant-your-key-here
