# Caches / build / deps (subdirs also ignore their own)
.dukascopy-cache/
node_modules/
.next/
out/
build/

# Large regenerable market data (rebuilt via the data pipeline).
# NB: keep these specific — a bare **/data/ also matches src/app/api/data/.
**/public/data/
**/data-pipeline/data/

# Python
__pycache__/
*.pyc
.venv/

# Misc
*.log
.DS_Store
*.tsbuildinfo
